{
  "name": "dcc-gongfarmers-classes-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for the DCC Gongfarmer's Almanac Classes module",
  "private": true,
  "scripts": {
    "install:browsers": "npx playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
